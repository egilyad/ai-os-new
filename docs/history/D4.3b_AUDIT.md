# D4.3b — castAudienceVote Canonical — Static Audit

> Дата: 2026-09-07. Scope: один метод `castAudienceVote` → `DebateEngine + WeightedJudgeEvaluator` (councilMode), old `councilVotes kind:audience` NOT called when engine exists. Без Dexie mass migration, без удаления `council*`.

---

## 1) Изменённые файлы

| Файл | Что |
|------|-----|
| `council/weighted-judge-evaluator:6` | `+audienceVotes: Map<sessionId, Map<voterId, pickId>>` + `recordAudienceVote(sessionId,voterId,pickId)` + `tallyAudience(sessionId): Map<pickId,count>` — one `IDebateEvaluator` (councilMode: audience advisory) |
| `council/council-service-facade:64` | `castAudienceVote: if debateEngine → oldSession.allowAudienceVoting check (read-only) → weightedJudge.recordAudienceVote → return AudienceVote {id, sessionId, voterId, pickId} — old.castAudienceVote NOT called` + `getSession merge audienceMap` (like judge merge) |

**Не изменены:** `council-service:343` old `councilVotes kind:audience` kept read-only rollback, `IDebateEngine` already `createSessionWithId` (D4.3a), `CouncilPanel` still `councilService.castAudienceVote` same `ICouncilService` facade API, no new `IAudienceService`, no new `DebateStore` row for audience (Map in-memory, Dexie persistence RUNTIME-PENDING per GO).

---

## 2) Точка интеграции — та же, что D4.3a (один evaluator)

```
CouncilPanel (audience vote)
  ↓ ICouncilService.castAudienceVote
  ↓ Facade (read old.allowAudienceVoting, then canonical)
  ↓ WeightedJudgeEvaluator.recordAudienceVote → audienceVotes Map (sessionId→voterId→pickId)
  ↓ getSession merge old base.votes + evaluator audienceMap → votes[] (CouncilPanel sees canonical vote)
  → conclude future will use `tallyAudience` for tie-break (same as CouncilService:373)
```

**Old path NOT called:** `council-service:343` `councilVotes put kind:audience` **not executed** when `debateEngine` exists ✅ (old read-only for weight check only).

---

## 3) Что реально теперь влияет

- **Now:** `WeightedJudgeEvaluator` audience Map canonical for `castAudienceVote`; `getSession` merges into `CouncilSession.votes[]` so `Panel/detail` + `conclude` advisory sees canonical audience votes.
- **Not yet:** `conclude` still old tally `359` `Map winnerId→weight` + audience `373` counts over `session.votes` (which now includes merged canonical votes via `getSession`, so same tally result) — but `conclude` still writes `winnerId/summary` to `councilSessions` old, not `DebateStore version CAS` (D4.3c).

---

## 4) Что осталось experimental / RUNTIME-PENDING

- Audience `tallyAudience` for tie-break `conclude` advisory — `RUNTIME-PENDING` until `conclude` migrated to `DebateEngine` consensus (D4.3c).
- `castAudienceVote` Dexie persistence for canonical (currently Map in-memory, not `DebateStore` row) — `RUNTIME-PENDING` strong PC `DebateStore` write + `version CAS` + `Timeline DEBATE_*` (per GO constraints: НЕ мигрировать Dexie now).

---

## 5) Статические проверки

| Check | Evidence | Result |
|-------|----------|--------|
| One evaluator | `WeightedJudgeEvaluator implements IDebateEvaluator` + `recordAudienceVote/tallyAudience` extra (not interface, same class) | ✅ one `IDebateEvaluator` (councilMode) |
| Old not called | `facade castAudienceVote: if debateEngine → weightedJudge.record… return vote` early return, `old.castAudienceVote` only fallback when `!debateEngine` | ✅ |
| No new persistence | No `version(28).stores`, no `council*` DELETE, no `DebateStore kind='council'` bulk | ✅ |
| Imports | `councilService:343` old `allowAudienceVoting` check read-only preserved | ✅ |

---

## 6) No second runtime/pipeline

| Check | Evidence | Result |
|-------|----------|--------|
| `IDebateEngine` | one engine, `WeightedJudgeEvaluator` is `IDebateEvaluator` strategy (not `IAudienceService`) | ✅ |
| `IConsensusEngine` | `setArgTechBonus` existing (D2.2) not duplicated | ✅ |
| `Provenance` | `IProvenanceService` wave10 reused | ✅ |

---

## GO / NO-GO

### 🟢 PASS — D4.3b `castAudienceVote` canonical — `CouncilPanel → Facade → WeightedJudgeEvaluator (councilMode)` without old `councilVotes kind:audience` write

**STOP — жду вердикта перед D4.3c `conclude`.**
