# T3.1 POST Audit — RU-роли учёных (5 шт)

> Дата: 2026-09-09. Scope: seed + wire RoleLibrary. Без persona-сидов (отдельно).

## Что сделано
1. **Новый `src/data/role-library.ts`**: `LibraryRole` + `LIBRARY_ROLES` (5) + `getLibraryRolesByCategory`.
   - Инженер, Физик, Математик, Химик, Биолог — имя/описание/systemPrompt на русском, temperature 0.4–0.5, tags RU, category `analysis`.
   - SystemPrompt у каждого со своей профдеформацией: инженер — изготовить/цена/где сломается; физик — порядки величин и размерности; математик — определения и контрпримеры; химик — безопасность; биолог — контрольные группы, корреляция≠причина.
2. **`RoleLibrary.tsx`**: заглушка заменена импортом из сида. Типы совпадают 1:1 (категории, recommendedModel?, capabilities).

## Проверки (static)
| Check | Evidence | Result |
|-------|----------|--------|
| Поля для install | `handleInstall` читает name/description/systemPrompt/baseTemperature/capabilities/tags — всё есть в сиде | ✅ |
| Категории | `CATEGORIES` + `CATEGORY_BADGE` + `CATEGORY_MAP` покрывают все 6, сид использует `analysis` | ✅ |
| Persona-заглушки | `debate-archetypes`, `historical-figures`, `PersonaPickerPanel` — не тронуты, остаются пустыми | ⏳ следующий batch |
| Контракты/таблицы | не тронуты | ✅ |

## Как проверить
1. Roles → Library → фильтр Analysis → 5 русских ролей.
2. Install химика → появляется в ролях, ставится агенту.
3. Агент с ролью отвечает по-русски в стиле роли.

**STOP — дальше T3.2 (карточка агента) или добивка persona-сидов, только по GO.**
