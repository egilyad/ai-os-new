# T1.2 POST Audit — Rotation Panel (view + configuration)

> Дата: 2026-09-09. Scope: новый таб `rotation` в ProviderManager. Без новых runtime/контрактов/таблиц.

## Что сделано
1. **Новый `ProviderManager/RotationView.tsx`** — 4 секции:
   - Три механизма словами (per-request pick / session affinity / TTL secret rotation).
   - Живая таблица на провайдера: keys on/total, pool strategy (селект), pool used, default model (SSOT), debate models (`DEBATE_MODEL_PRIORITY`), debate order (readout хардкода `router-debate-selector`).
   - TTL secret rotation: таблица ключей с таймерами + Set/Cancel/Rotate now.
   - Session affinity snapshot (топ-20 + статус из keyStateStore).
2. **Таб `rotation`** в `ProviderManagerView` (TabId/TABS/chain/render) + `ProviderManagerContainer` TABS (стрелочная навигация — автоматом).
3. **Конфигурация — только существующие сеттеры:** `keyService.setPoolStrategy` (persist via saveConfig), `rotationService.setKeyTTL/cancelRotation/rotateNow`. SLA не дублировали (уже в routing-табе).

## Проверки (static, weak PC)
| Check | Evidence | Result |
|-------|----------|--------|
| `PoolStrategy` тип | `contracts/pool-selector.ts:3` round-robin/least-usage/random | ✅ |
| `keyService.getPoolStrategy/setPoolStrategy/getPoolStatus` | `key-service.ts:710-798` | ✅ |
| `IRotationService` методы | `contracts/key-rotation.ts:14-19` setKeyTTL/cancel/rotateNow/getTTLStatus | ✅ |
| `SessionBinding` поля | `contracts/session-affinity.ts:1-9` sessionId/keyId/provider | ✅ |
| `usePolling` путь | `components/Common/usePolling.ts` существует, `../Common/usePolling` из ProviderManager | ✅ |
| `keyStateStore.get()?.status` | тот же паттерн что `SessionBindingsPanel.tsx:40` | ✅ |
| `keyService/rotationService/sessionAffinityStore/keyStateStore` в instances | `core-references.ts:5,74,127`, `services-extras.ts:609` | ✅ |
| Новый i18n-ключ не вводили (таб — plain 'Rotation') | нет `t('...rotation...')` | ✅ (без missing-key варнингов) |
| tsc/esbuild/vitest | weak PC душит процессы (ChildProcess.kill), полный tsc таймаутит | ⏳ NEEDS dev-boot |

## Не тронуто
T1.3/T1.4 (привязка агента), R-GAP-02, остальные GAP, N4 chain, debate provider order (readout only — делать конфигурируемым = отдельный batch с правкой селектора).

## Как проверить (на твоей машине)
1. `vite dev` → `/keys` → таб Rotation.
2. Сменить стратегию пула → перезагрузить → значение сохранилось (persist).
3. Поставить TTL 1h на ключ → строка active + countdown; Cancel → idle.

**STOP — жду вердикта.**
