# D4.5a — Persistent Evaluator State — Static Audit (без новой Dexie-таблицы)

> Дата: 2026-09-07. Scope: `WeightedJudgeEvaluator` judge+audience → `Timeline + keyValue CAS/checksum + DebateStore snapshot` persistent, one evaluator, idempotent, restore after reload. Без `CouncilMigrationService` bulk, без Dexie mass.

---

## 1) Изменённые файлы (только D4.5a, без второго runtime)

| Файл | Что |
|------|-----|
| `council/weighted-judge-evaluator:1` | `+deps?:{database,eventBus} 14`, `+init/restoreAll 27`, `restoreSession(sessionId) 31` `getKv council:judge/audience:${sessionId} → Map`, `recordJudgeVote async 41` `restoreSession` lazy + `persistJudge 46` `setKv council:judge:${sessionId} {votes:Object.fromEntries, checksum:JSON length hex, updatedAt}` + `emit council:judge:score`, `recordAudienceVote async 54` `persistAudience 46` `setKv council:audience:${sessionId}` + `snapshot best-effort` via `debateEngine.saveSnapshot` |
| `service-registration/phase3:224` | `weightedJudgeEvaluator` DI singleton `new WeightedJudgeEvaluator({database,eventBus})` — same instance for Engine + Facade |
| `council/council-aware-evaluator:1` | `isCouncilModeForCurrentCall` via `global __currentDebateSessionId` thread-local set in `debate-phase-handler:90` (cleared after), delegates `scoreArguments/rankParticipants` to `weighted` when `topology.nodes[0].config.councilMode` |
| `debate-runtime/debate-phase-handler:90` | `__currentDebateSessionId = sessionId` before `claims extract` → evaluator, cleared after `if(deps.evaluator) block` |
| `council/council-service-facade:20` | `weightedJudge` now injected `same instance` from `phase3/24` (`private weightedJudge: WeightedJudgeEvaluator` via `c.get('weightedJudgeEvaluator')` `phase24:25`), `getSession` `await restoreSession(id)` before merge `votes/audienceVotes` → `scores[]/votes[]`, `submitJudgeScore` `await recordJudgeVote` + `setKv council:judge:${sessionId} + emit + saveSnapshot`, `castAudienceVote` same `council:audience:${sessionId}` |

**Не изменены:** `council*` 3 tables v24 (no new table), `DebateStore` stringified `arguments` + `version CAS` (existing `persistence-manager:137`), `councilStore` (still 6 `council:*` subs, not `activeDebateStore`), `historical bulk` — не трогали.

---

## 2) Persistence contract — Timeline + keyValue CAS/checksum + snapshot (no new table)

```
write → recordJudgeVote → votes Map sessionId→judgeId → persistJudge
  → setKv council:judge:${sessionId} {votes: Object.fromEntries(Map), checksum: JSON length hex, updatedAt} (DatabaseService setKv 589, version CAS via keyValue `id,createdAt` 168)
  → emit council:judge:score (Timeline additive, not `councilVotes` table)
  → debateEngine.saveSnapshot(sessionId) → DebateStore.saveSnapshot(record stringified) + version CAS retry 3× 154 + Bucket RESEARCH slice(-500) 62 + timeline.persist 158
  → getSession merge: restoreSession(id) → getKv council:judge:${id} + council:audience:${id} → Map → mergedScores/votes
```

**Idempotent write:** `recordJudgeVote` `set` overwrites `judgeId` key, `persistJudge` `setKv` overwrites same `council:judge:${id}` with same `votes` payload → checksum same → second call with same `judgeId/winnerId` → no duplicate, same `payload` length hex.

**Timeline + keyValue + snapshot — как спроектировано D4.4 `D+C+A`:** TimelineEntry additive `council:judgeVote` not yet separate `type` but `emit council:judge:score` + `keyValue` + `snapshot` version.

---

## 3) Restore after reload — `write → persist → clear/recreate evaluator → load → restore → getSession`

**Static verification (без reload device, RUNTIME-PENDING for real reload):**

| Step | Evidence | Result |
|------|----------|--------|
| `write` | `recordJudgeVote(sessionId,judgeId,winnerId)` `votes.set` + `persistJudge setKv` | ✅ Map in-memory + keyValue |
| `persist` | `setKv council:judge:${sessionId} {votes, checksum}` `46` | ✅ keyValue |
| `clear/recreate evaluator` | `new WeightedJudgeEvaluator({database,eventBus})` `phase3` DI singleton — new instance empty Maps `14` | Simulate clear |
| `load` | `facade.getSession(id) → await weightedJudge.restoreSession(id) 31` `getKv council:judge:${id}` → `Map` | ✅ restore |
| `restore` | `restoreSession` `getKv` → `Map.set` `votes` + `audienceVotes` | ✅ Map restored |
| `getSession` | `getSession merge sessionVotes/audienceMap → mergedScores/votes` `58` | ✅ `Panel` sees canonical vote after reload |

**Checksum:** `checksum = JSON.stringify(payload).length.toString(16)` `46` — idempotent check (same votes → same checksum), not cryptographic but `RUNTIME-PENDING` for strong hash (e.g., `hashClaim` FNV).

**Snapshot:** `saveSnapshot` best-effort fire-and-forget `catch` — not blocking `submitJudgeScore` return, but `version CAS` ensures `DebateStore` snapshot eventually has `version+1` with `arguments` (Council `facts→arguments` already in snapshot via mapper, not judge votes — judge votes still via `keyValue`, not `arguments` stringified).

---

## 4) No second runtime/pipeline, old not called

| Check | Evidence | Result |
|-------|----------|--------|
| `IDebateEvaluator` | `WeightedJudgeEvaluator implements IDebateEvaluator` + `CouncilAwareEvaluator` one `IDebateEvaluator` (two strategies via `topology.config.councilMode`) | ✅ one evaluator |
| `Facade` | `weightedJudge` injected same instance `phase24:25` via `c.get('weightedJudgeEvaluator')` — not `private new` second Map | ✅ one Map (was two, now one) |
| `submitJudgeScore` | `if debateEngine → weightedJudge.recordJudgeVote → setKv + saveSnapshot → return` early, `old.submitJudgeScore` only fallback `!debateEngine` `144` | ✅ old `councilVotes kind:judge` NOT called when engine exists |
| `castAudienceVote` | same `recordAudienceVote` + `keyValue + snapshot` | ✅ |
| `conclude` | already canonical via `WeightedJudgeEvaluator tallyWinner 28` (D4.3c) + `getSession` merge | ✅ |
| `council*` | `councilSessions/Messages/Votes` v24 still `councilSessions/Messages/Votes` `dexie-schema:952`, no `DELETE`, read-only for rollback (`getSession` base) | ✅ not deleted |

---

## 5) Static checks

| Check | Evidence | Result |
|-------|----------|--------|
| `WeightedJudgeEvaluator` DI | `phase3 weightedJudgeEvaluator` + `phase24 facade` same `c.get('weightedJudgeEvaluator')` | ✅ one instance |
| `CouncilAwareEvaluator` | `phase3 councilAwareEvaluator` wraps `standard + weighted` + `getSession` lazy `topology.nodes[0].config.councilMode` | ✅ no second engine |
| `keyValue` | `database setKv council:judge/audience:${id} {votes, checksum}` via `DatabaseService setKv 589` | ✅ CAS via `keyValue` version `168` |
| `Timeline` | `emit council:judge:score` additive, not `councilVotes` table | ✅ |
| No new table | `dexie-schema:205 council*` still 3 tables, `version(28) 1121` additive, no `councilSessions:null` drop | ✅ |
| `round` | still heuristic `fact_gathering 0` (pre: D4.3) — not fixed in D4.5a (as scoped) | 🟡 heuristic, RUNTIME-PENDING for dynamic `session.messages.filter(forum).length` |

---

## GO / NO-GO

### 🟢 PASS — D4.5a Persistent Evaluator State — `write → persist (Timeline+keyValue+snapshot best-effort) → clear/recreate → load → restore → getSession` static verification complete, one `WeightedJudgeEvaluator`, old judge/audience `councilVotes` NOT called, no new Dexie table, no bulk migration.

**RUNTIME-PENDING:** Real reload `clear evaluator → restoreSession getKv → Map` + `saveSnapshot version CAS 3× retry 154` + `Bucket RESEARCH` persistence on strong PC + `Timeline` replay `loadPersisted 346`.

**STOP — жду вердикта перед D4.5b Migration gate/checksum/rollback (без bulk).**
