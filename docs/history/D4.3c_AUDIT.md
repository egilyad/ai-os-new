# D4.3c — conclude Canonical — Static Audit

> Дата: 2026-09-07. Scope: один метод `conclude` → `WeightedJudgeEvaluator` canonical tally (judge weight + audience advisory), old tally NOT called when engine exists. Без Dexie mass migration, без удаления `council*`.

---

## 1) Изменённые файлы

| Файл | Что |
|------|-----|
| `council/council-service-facade:147` | `conclude(sessionId): if debateEngine → base = old.getSession → winnerId = weightedJudge.tallyWinner(sessionId) + audience tie-break via weightedJudge.tallyAudience(sessionId) (same as old CouncilService:359 tally Map winnerId→weight + 373 audience counts) → fallback to old base scores/votes if evaluator empty (before D4.3a/b votes) → summary `Winner: ${winner.name} (canonical via WeightedJudgeEvaluator)` → putSession via repo directly (bypass old tally re-run) + emit COUNCIL_COMPLETED` |
| `council/weighted-judge-evaluator:30` | Already `tallyWinner + tallyAudience` from D4.3b |

**Не изменены:** `council-service:355` old `tally 359` kept read-only fallback (not called when engine exists), `IDebateEngine` already `createSessionWithId` (D4.3a P0), `CouncilPanel` still `councilService.conclude` same `ICouncilService` facade API, no new `IConcludeService`, no new `DebateStore` row for winner (still councilSessions mirror, DebateStore canonical result D4.5).

---

## 2) Точка интеграции — та же, что D4.3a/b (один evaluator)

```
CouncilPanel (conclude)
  ↓ ICouncilService.conclude
  ↓ Facade (read base getSession read-only, NOT old tally)
  ↓ WeightedJudgeEvaluator.tallyWinner(sessionId) + tallyAudience(sessionId) — Map winnerId→weight (same 359) + advisory tie-break (same 373)
  ↓ winnerId + summary → putSession via repo (old persistence as mirror, not second judging) + emit COUNCIL_COMPLETED
  → DebateStore canonical result (via evaluator, not old tally) → future D4.5 DebateStore version CAS
```

**Old tally NOT called:** `council-service:355` `tally Map winnerId→weight 359` **not executed** when `debateEngine` exists — facade early computes via `weightedJudge`, then writes `updated: winnerId/summary/phase=completed/status=completed` directly via `repo.putSession` (bypass `old.conclude` re-tally). Old `council*` remains read-only mirror, not second judging.

---

## 3) Что реально теперь влияет

- **Now:** `conclude` winner via `WeightedJudgeEvaluator` votes Map (from `recordJudgeVote` D4.3a) + `tallyAudience` (D4.3b) — same weighted tally + audience advisory as old, but via canonical `IDebateEvaluator` (councilMode). `getSession` merge already shows canonical votes, so `conclude` sees same votes.
- **Not yet:** `conclude` still writes `winnerId/summary` to `councilSessions` old (mirror, not `DebateStore version CAS` canonical) — `DebateStore kind='council' version` persistence RUNTIME-PENDING (strong PC, GO said НЕ мигрировать Dexie now). `summary` still `Winner: name (canonical via WeightedJudgeEvaluator)` — provenance trace still old (D4.3 provenance already wired to FactPacket).

---

## 4) Что осталось experimental / RUNTIME-PENDING

- `conclude` Dexie persistence for canonical (currently `repo.putSession` old `councilSessions` mirror, not `DebateStore` row `version CAS` + `Bucket`) — RUNTIME-PENDING strong PC `DebateStore kind='council'` write + `Timeline DEBATE_CONSENSUS_REACHED` (per GO constraints: НЕ мигрировать Dexie now).
- `round` dynamic for `completed→maxRounds` still hard-coded `86` (pre: fact_gathering 0) — RUNTIME-PENDING `session.messages.filter(forum).length`.
- No new `IDebateEvaluator` strategy switch via `topology.metadata.councilMode` flag yet — `WeightedJudgeEvaluator` is standalone, not yet registered as `DebateEngine` evaluator alternative (still facade-level tally, not `DebateEngine` consensus pipeline).

---

## 5) Статические проверки

| Check | Evidence | Result |
|-------|----------|--------|
| One evaluator | `WeightedJudgeEvaluator implements IDebateEvaluator scoreArguments/rankParticipants` + `recordJudgeVote/recordAudienceVote/tallyWinner/tallyAudience` extra (same class, not second runtime) | ✅ one `IDebateEvaluator` (councilMode) |
| Old tally NOT called | `facade conclude: if debateEngine → weightedJudge.tallyWinner → putSession via repo` early return, `old.conclude` only fallback when `!debateEngine` or catch | ✅ old `tally 359` not second source |
| No new persistence | No `version(28).stores`, no `council*` DELETE, no `DebateStore kind='council'` bulk, `repo.putSession` old mirror only | ✅ |
| Imports | `council-service:355` old tally kept read-only, `WeightedJudgeEvaluator` lazy in facade | ✅ |

---

## 6) No second runtime/pipeline

| Check | Evidence | Result |
|-------|----------|--------|
| `IDebateEngine` | one engine, `WeightedJudgeEvaluator` is `IDebateEvaluator` strategy (not `ICouncilConcludeEngine`) | ✅ |
| `IConsensusEngine` | `setArgTechBonus` existing (D2.2) not duplicated | ✅ |
| `Provenance` | `IProvenanceService` wave10 reused | ✅ |

---

## GO / NO-GO

### 🟢 PASS — D4.3c `conclude` canonical tally — `CouncilPanel → Facade → WeightedJudgeEvaluator (same 359 weighted tally + 373 audience)` without old `tally` as second source

**Contract Repair D4.3a/b/c — ALL PASS 🟢** — `submitJudgeScore` ✅, `castAudienceVote` ✅, `conclude` ✅ — one `IDebateEvaluator` councilMode, old `council*` judge/vote/tally NOT called when engine exists, no second judging/runtime.

**STOP — жду вердикта перед D4.3 migration (Dexie/store/history) — как просил, не давать GO на массовую миграцию до проверки.**
