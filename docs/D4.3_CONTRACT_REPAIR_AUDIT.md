# D4.3 CONTRACT REPAIR AUDIT — Static Verification (без миграции Dexie/store/history)

> Дата: 2026-09-07. Scope: 6 условий P0 batch `docs/D4.3_CONTRACT_REPAIR_PLAN.md`. Проверка: ID round-trip, 5→11 preservation, facts/messages round-trip, lens propagation, double-blind preservation, judge/vote canonical path, no second pipeline, no new persistence.

---

## 1) ID round-trip — council-abc ↔ debate-abc same canonical

| Check | Evidence | Result |
|-------|----------|--------|
| `IDebateEngine` contract | `debate-runtime:346` `createSessionWithId(id, topology, topic, parts, lang)` — minimal extension (overload, not new runtime, delegates `createSession:261` → `createSessionWithId(genId)`) | ✅ same API, no parallel runtime |
| Mapper ID same | `council-to-debate-mapper:81` `id: session.id` preserved | ✅ |
| Facade canonical ID | `council-service-facade:32` `createSessionWithId(session.id, snapshot.topology, ...)` — not `genId debate-xyz` (old dual-write `debate-xyz` fixed), fallback `?: createSession` for backward compat, `Map<councilId,debateId>` not needed when `createSessionWithId` exists (kept as comment for historical) | ✅ split-brain removed |
| No Map/keyValue needed for new sessions | `createSessionWithId` pass-through canonical `council-abc` | ✅ (Map kept only for historical `debate-xyz` legacy) |

**Verdict:** 🟢 PASS — new sessions `council-abc` == `debate-abc`, no split-brain for new, historical `Map` still needed for legacy `debate-xyz` (read-only rollback).

---

## 2) 5→11 state preservation

| Council | → Debate | Guard | Recoverable |
|---------|----------|-------|-------------|
| `proposal` | `created` | `StateMachine can(queued)` needs `initializing→active` gap — Council jumps directly | 🟡 `fact_gathering` distinguishable via `topology metadata factGathering` but not via phase |
| `fact_gathering` | `deliberating` (restricted) | Not in mapper phase table alone — `topology config factGathering` carries metadata | ✅ metadata preserved (§4 topology config) |
| `debate` | `deliberating` | Same as above, but `paused` lost forward | 🟡 `paused` lost (no Council pause) |
| `consensus` | `consensus` | `summarizing` hidden | ✅ |
| `completed` | `completed` | `round: maxRounds` hard-coded `86` loses actual round count | 🟡 round hard-coded |
| `aborted` | `cancelled` | `failed` merges into `aborted` | 🟡 `failed` (zombie 5min) merges |

**Verdict:** 🟡 PARTIAL — table `councilPhaseToDebatePhase:13` preserved 5→11 with metadata, but `round` still hard-coded `0/maxRounds` (needs `session.messages.filter(forum).length` dynamic) and `paused` not representable — acceptable for D4.3 pre-migration, `RUNTIME-PENDING` for full `StateMachine send` parity.

---

## 3) Facts/messages round-trip

| Source | Mapper `council-to-debate-mapper:62` | Preserved |
|--------|--------------------------------------|-----------|
| `FactPacket claim` | `→ Claim.text content` | ✅ full claim (not slice 280) |
| `verdict verified 0.9 / disputed 0.2 / unverifiable 0.5` | `→ confidence` | ✅ heuristic documented |
| `sources: string[]` | `→ citations + evidence = sources[0]` | ✅ `citations` + `evidence` |
| `authorId` | `→ agentId + speaker` | ✅ |
| `author.kind` | `→ role` | ✅ |
| `createdAt` | `→ timestamp` | ✅ |
| `round` | facts `0` heuristic, messages `m.round??0` + sorted timestamp `sort((a,b)=>timestamp)` | 🟡 facts round hard-coded (pre: fact_gathering 0) |
| `whisper` | `messages.filter(channel==='forum')` **fixed** → `messages.map` all channels `70` + sorted, `channel/toId` comment preserved via payload | ✅ not filtered, channel preserved via timeline payload (see §4) |
| Interleaving | `[...facts,...messages].sort(timestamp)` | ✅ timestamp sorted |

**Verdict:** 🟢 PASS static — facts/messages → `arguments[]` round-trip with citations/evidence/speaker/role/round/whisper preserved, heuristic confidence documented, no web grounding (RUNTIME-PENDING per plan).

---

## 4) Lens/polarity propagation — real systemPrompt

| Check | Evidence | Result |
|-------|----------|--------|
| `councilToTopology` config | `30: {lensId,polarityId,councilMode, doubleBlind, aliases}` | ✅ metadata preserved |
| `councilParticipantsToDebateParticipants` | `44: systemPrompt = buildParticipantPrompt({name,kind,lensId,polarityId}) via require('./council-lenses')` | ✅ real prompt, not metadata-only (was `undefined 51`) |
| `DebateEngine.createSession` stores `systemPrompt` verbatim `254` → `buildDebateCallContext:106` `systemPrompt:p.systemPrompt` → `buildDebateSystemContent` | ✅ reaches LLM pipeline (was dead) |
| `LensPromptMiddleware` shared lib | `council-lenses:6 no dependencies` reused — not duplicated `debate-archetypes` | ✅ metadata-only propagation NOT counted as fix (now fixed) |

**Verdict:** 🟢 PASS — `lensId/polarityId` now real `systemPrompt` (not metadata-only).

---

## 5) Double-blind metadata preservation

| Field | Mapper `30` | Result |
|-------|-------------|--------|
| `doubleBlind` flag | `config:{doubleBlind: session.config.doubleBlind}` | ✅ |
| `aliases: pid→Speaker` | `config:{aliases: session.aliases}` | ✅ |
| `channel forum|whisper + toId` | `messages.map` all, comment `channel/toId preserved via payload in timeline` — `TimelineEntry.payload unknown` already carries `channel` | ✅ (was dropped `70`) |
| `blind` judge flag | `scores[].blind` mapped via `WeightedJudgeEvaluator`? Still via old `councilVotes kind:judge payload.blind` — preserved in mapper? Not yet in `arguments[]`, but `conclude` will carry via `WeightedJudgeEvaluator` (see §6) | 🟡 `blind` in `JudgeScore` not yet in `Claim` but in `DebateStore scores` after migration |

**Verdict:** 🟢 PASS static — aliases/doubleBlind/channel/toId/blind now in `topology.config` + `TimelineEntry payload` via adapter (no second messaging runtime, `WhisperChannelAdapter publicId` reuse as planned in §5 plan).

---

## 6) Judge/Vote/Conclude canonical path — no second pipeline

| Method | Before (old only) | After D4.3 | Canonical? | Second pipeline? |
|--------|-------------------|------------|------------|------------------|
| `createSession` | `old.createSession → council*` | `old.createSession + debateEngine.createSessionWithId(council-abc)` dual-write with same ID | ✅ DebateStore canonical after fix, old read-only rollback | ✅ no |
| `submitJudgeScore` | `old → councilVotes kind:judge + tally` | Still `old` (facade:64 `old.*`) — **not yet migrated** to `DebateEngine` | 🟡 still old, **prepare** `WeightedJudgeEvaluator implements IDebateEvaluator 279` exists `weighted-judge-evaluator:1` selectable via `topology.metadata.councilMode` — **not yet wired** to `facade.submitJudgeScore` | ✅ evaluator exists, not second runtime (one `IDebateEvaluator` interface, two strategies) |
| `castAudienceVote` | old | Still old | 🟡 same | ✅ |
| `conclude` | old tally `359` `Map winnerId→weight` + audience tie-break `373` → `winnerId/summary` | Still old, but `WeightedJudgeEvaluator rankParticipants sort by overall` prepared as alternative to `BayesianJudge` | 🟡 `conclude` still old tally, `WeightedJudgeEvaluator` ready for `consensusAndFinalize 371` switch | ✅ one `Evaluator` interface |
| `submitProposal/submitFact/postMessage/advancePhase/abort` | old | Still old (facade `64`) | 🟡 still old, will delegate to `DebateTimeline.record channel/toId` + `memory.claims` + `StateMachine.send` after D4.3 preflight | ✅ no second engine |

**Check:** `WeightedJudgeEvaluator implements IDebateEvaluator` `scoreArguments/rankParticipants` — uses same `IDebateEvaluator` contract `debate-runtime:269` as `BayesianJudge` `debate-engine:279`; selectable via `councilMode` flag, **one pipeline** (`DebateConsensusEngine + Evaluator + Provenance → Result`), not `Council judging + Debate judging + WeightedJudge` triple.

**Verdict:** 🟡 PARTIAL — **evaluator exists, not yet wired** to `facade.submitJudgeScore/conclude` (kept old for D4.3 Contract Repair without Dexie migration per GO constraints: `НЕ удалять council* tables`, `НЕ делать mass migration`). No second `DebateEngine`/`ConsensusEngine` created — **PASS no second runtime**, but canonical path **not yet active** for judge/vote/conclude (still old) — RUNTIME-PENDING until D4.3 migration.

---

## 7) No second judging/runtime pipeline

| Check | Evidence | Result |
|-------|----------|--------|
| `IDebateEngine` | `createSessionWithId` overload, not new `ICouncilEngine` | ✅ one engine |
| `IDebateEvaluator` | `WeightedJudgeEvaluator implements IDebateEvaluator` (not `ICouncilJudgeEngine`) | ✅ one evaluator interface, two strategies |
| `IConsensusEngine` | `DebateConsensusEngine setArgTechBonus` existing (D2.2) — not `CouncilConsensusEngine` | ✅ one consensus |
| `EventBus` | `COUNCIL_* 10` + `DEBATE_* 30+` unchanged, `ArgTech` events `DUNG_ATTACK` reused | ✅ no new bus |
| `Provenance` | `council-service:211` uses existing `IProvenanceService addNode/link` (wave10 tables) | ✅ no new model |

**Verdict:** 🟢 PASS — no second runtime/pipeline created.

---

## 8) No new persistence models

| Check | Evidence | Result |
|-------|----------|--------|
| `Dexie` | No new tables `version(28).stores` not added; `provenanceNodes/Edges` v30 reused; `council*` still `councilSessions|Messages|Votes` v24 read-only | ✅ no new model |
| `DebateStore` | `version CAS` + `Bucket RESEARCH` `debate_timeline_${id}` reused, `kind='council'` discriminator in `topologyType` (exists `dexie-schema:354`) | ✅ one store |
| `keyValue` | `council:map:<id>` not created yet (not needed when `createSessionWithId` same ID) — historical map only for legacy `debate-xyz` (comment in facade) | ✅ minimal |

**Verdict:** 🟢 PASS — no new persistence models, no mass historical migration, rollback layer intact.

---

## GO / NO-GO

### 🟡 PARTIAL — Contract Repair завершён частично, можно планировать D4.3 migration, но с блокерами:

**PASS (можно планировать D4.3 implementation):**
- P0 ID canonical same `council-abc` via `createSessionWithId` — split-brain removed for new sessions ✅
- Lenses `lensId/polarityId` → `systemPrompt` real (not metadata) ✅
- Facts→arguments semantic mapper (sources→citations, whisper included, sorted, role/speaker preserved) ✅
- Double-blind `aliases/doubleBlind/channel/toId` via `topology.config + Timeline payload` ✅
- No second runtime/pipeline, no new persistence ✅

**Оставшиеся blockers (RUNTIME-PENDING до D4.3 migration):**
- Judge/Vote/Conclude still old `councilVotes` tally (evaluator prepared but not wired to `facade.submitJudgeScore/conclude`) — need to delegate `submitJudgeScore/castAudienceVote/conclude` via `WeightedJudgeEvaluator` + `DebateStore` `version CAS` + `timeline` before `DebateStore` can be SSOT (see §6 table).
- `round` hard-coded for facts `0` and `completed→maxRounds` approximation (§2) — needs dynamic `session.messages.filter(forum).length`.
- Historical `council-xyz` vs `debate-xyz` legacy Map for old dual-write sessions (phase D4.2 before fix) still needs `keyValue` map for `getSession` routing.

**• Если исправить judge/vote/conclude delegation (one evaluator via `councilMode` flag) + `round` dynamic → 🟢 CONTRACT SAFE**
**• Сейчас → 🟡 PARTIAL — D4.3 Contract Repair batch PASS, D4.3 migration NO-GO until judge path wired**

С богом — audit без кода, только контракт. Следующий этап — выбрать: `D4.4 Judge migration` (one method at a time) vs ждать сильного ПК — после твоего вердикта.
