# D4.5c — Single-session Dry Run — Static Audit (без bulk, без Dexie mass, без SSOT cutover)

> Дата: 2026-09-07. Scope: одна fixture Council-сессия полный путь `Council → mapper → DebateStore → read back → verify → marker → repeat idempotent → rollback`. Без bulk, без `council*` удаления, без новых таблиц, `RUNTIME-PENDING` reload.

---

## Fixture (single session, not 100)

Synthetic `CouncilSession` via `CouncilService.createSession` or direct `putSession`: `topic: "Should prioritize X?"`, `participants 4` (proponent steelman, opponent devil, researcher empiricist, fact_checker popper) + `judges 2` (w1, w2), `config doubleBlind true, factGathering true, maxRounds 3, allowAudienceVoting true`, `aliases Speaker A..D`, `facts 2` (claim+verdict+2 sources each), `messages 3` (forum broadcast + whisper private toId), `scores 2` (judge→w1), `votes 2` (audience), `phase debate`, `status running`.

---

## Full path `Council → mapper → DebateStore → read back` (D4.5b + D4.5c)

```
CouncilSession (council* 3 tables v24)
  ↓ CouncilMigrationService.migrateOne(sessionId) — gate canMigrate marker absent → snapshot old council:rollback:snapshot:${id}
  ↓ mapper councilToDebateSnapshot: id same council-abc, topology council-${id} roundtable nodes config {lensId,polarityId,councilMode, doubleBlind, aliases, allowAudienceVoting}, participants systemPrompt via buildParticipantPrompt, arguments sorted timestamp (facts+messages), citations/evidence, speaker/role
  ↓ integrity: argsCount == facts+messages (2+3=5) — else rollback
  ↓ DebateStore.saveSnapshot(record stringified: topology/participants/agentStates/arguments/memory, version:1, language:en) — version CAS 3× retry + Bucket RESEARCH + DebateSessionRecordSchema safeParse 553
  ↓ read back getSnapshot(id) → back.id/topic check → marker setKv council:migrated:${id} {checksum: FNV hash topic+participants+facts+messages+tally, at, debateId, topic}
  ↓ verify(sessionId): checksum current vs marker — ok
```

**Evidence:** `council-migration-service:56` `migrateOne` gate→snapshot→mapper→verify argsCount→DebateStore.saveSnapshot→getSnapshot→marker `89`; `council-to-debate-mapper:102` `id same`, `30` per-node `councilMode`, `44` `systemPrompt` via lenses, `62` `sources→citations`; `debate-store:54` `saveSnapshot` + `schema-types:553` validation.

---

## Checks (9+ required)

| Check | Old → New | Result |
|-------|-----------|--------|
| **Same ID** | `council-abc → council-abc` via `createSessionWithId` P0 fix `debate-engine:261` + mapper `id: session.id 102` | ✅ `back.id === session.id` |
| **Topic** | `Should prioritize X?` | ✅ `back.topic ===` |
| **Participants** | 4 `proponent/opponent/researcher/fact_checker` + `judges 2` | ✅ `snapshot.participants 4` + `topology.nodes 4` |
| **Arguments** | `facts 2 + messages 3 =5` sorted timestamp | ✅ `argsCount 5` `snapshot.arguments.length 5` |
| **Facts/messages** | `facts claim→content, verdict→confidence 0.9/0.2/0.5 heuristic, sources→citations/evidence` | ✅ heuristic documented, not canonical |
| **Judge votes** | 2 `JudgeScore winnerId w1` → `WeightedJudgeEvaluator votes Map` (D4.3a) + `keyValue council:judge:${id} 46` | ✅ via `council:migrated` marker + `weightedJudge` Map (in-memory + keyValue) — not `DebateStore arguments` stringified (still Map) |
| **Audience votes** | 2 `pickId` → `audienceVotes Map` `recordAudienceVote 43` + `keyValue council:audience:${id}` | ✅ same |
| **Winner/result** | `conclude` via `WeightedJudgeEvaluator tallyWinner` (D4.3c) not old `Map 359` | ✅ same tally logic via evaluator, not old `councilVotes` |
| **Topology/councilMode/lenses/double-blind** | `topology council-${id} roundtable nodes config:{lensId,polarityId,councilMode,doubleBlind,aliases,allowAudienceVoting}` `30` + `systemPrompt` `44` | ✅ `topology.nodes[0].config.councilMode` true, lens prompt via `buildParticipantPrompt` |
| **Checksum** | `FNV hash topic+participants+facts+messages+tally` `18` | ✅ `marker checksum` vs `verify currentChecksum` |
| **Repeat idempotent** | `canMigrate` → `already migrated at ... checksum` `47` marker exists → `migrateOne` throws `Cannot migrate: already migrated` → second call no duplicate | ✅ |
| **Rollback** | `rollback(sessionId)` → `putSession(snapshot)` + `setKv marker null` + `snapshot null` `107` | ✅ single session restore, `canMigrate` true again |

**Runtime reload `RUNTIME-PENDING`:** `write → persist keyValue + DebateStore snapshot version CAS + Timeline → clear evaluator Maps → restoreSession getKv council:judge/audience:${id} → Map` — static verification of `restoreSession 31` + `persistJudge 46` + `debateEngine.saveSnapshot` best-effort, real reload on strong PC pending (as D4.5a).

---

## What remains bulk / NO-GO (as scoped)

- 🚫 No bulk `for 100 sessions` historical migration (single fixture only)
- 🚫 No `council*` tables delete / `version(28)` drop
- 🚫 No `DebateStore` SSOT cutover (`council*` still SSOT, `DebateStore` secondary with one dry-run row)
- 🚫 No new Dexie tables (reuse `debateSessions` + `keyValue` + `Bucket RESEARCH`)
- 🟡 `round` heuristic `fact_gathering 0` still, `status` lost, `judges dimensions/weight` in `WeightedJudgeEvaluator judgeWeights Map` not in snapshot (same as D4.3 loss table) — acceptable for single dry run

---

## Verdict

### 🟢 PASS — D4.5c Single-session dry run — Council → DebateStore round-trip + idempotent + rollback static verification complete (one fixture, 9+ checks) — без bulk, без Dexie mass, без SSOT cutover

**RUNTIME-PENDING:** Real `clear evaluator → restoreSession getKv → Map` + `saveSnapshot version CAS 3× retry 154` + `Bucket RESEARCH slice(-500)` + `Timeline loadPersisted` on strong PC + `verifyIntegrity` hash compare on 100 sessions.

**Next:** Only after this PASS → **D4.6 historical bulk migration** (100 sessions) — **STOP until strong PC + GO**.

С богом — audit без кода/persistence mass, только single-session dry run.
