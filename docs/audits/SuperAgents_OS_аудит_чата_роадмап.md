# Аудит чата + роадмап (из XLSX)

> Конвертировано из `SuperAgents_OS_аудит_чата_роадмап.xlsx`. Таблицы — как в исходнике, без правок.

## Сводка

| Глубокий аудит чатовой подсистемы — SuperAgents OS |  |  |  |  |
|---|---|---|---|---|
| Паспорт аудита |  |  |  |  |
| Объект | Чатовая подсистема SuperAgents OS v4.5.0 (egilyad/ai-os-new) |  |  |  |
| Ветка / коммит | fix-debate-text-truncation / d632cf7 |  |  |  |
| Дата аудита | 2026-09-18 |  |  |  |
| Глубина | Построчный разбор ядра чата + typecheck + прогон тестов |  |  |  |
| Метод | 3 линии аудита (kernel-сервисы / LLM-слой / store+UI+персистентность), личная верификация каждой критической находки по исходникам |  |  |  |
| Охват | ChatPanel (22 файла, ~4 500 LOC), stores/chat (9 файлов, ~1 670 LOC), 15+ kernel-сервисов (~3 300 LOC), LLM-слой (адаптеры, SSE, декораторы), DAL, конфиги прокси/CSP |  |  |  |
| Инструменты | tsc -b --noEmit (полный), vitest run (чатовые сьюты), ручной трейс событийных потоков |  |  |  |
| Итоговая оценка здоровья чата | 5.6 / 10 — ядро продуманное, но стриминг/прокси/персистентность содержат блокирующие дефекты |  |  |  |
| Статистика находок |  |  |  |  |
| Всего | Критических | Высоких | Средних | Низких |
| 45 | 4 | 13 | 18 | 10 |
| Оценка здоровья по слоям (0–10) |  |  |  |  |
| Слой | Оценка | Комментарий |  |  |
| UI-компоненты (ChatPanel) | 6.5 | Сильный XSS-пост (DOMPurify, sandbox-iframe); минусы: виртуализатор без измерения высоты, пер-токеные ре-рендеры, {ms}-плейсхолдер, 34 захардкоженные строки |  |  |
| Store (Zustand, stores/chat) | 6 | Хорошая структура и write-through, но очередь теряет сообщения, stale-active-session гонки, offset-пагинация ломается |  |  |
| Персистентность (Dexie/hydration) | 5 | Фантомная сессия 'default' — потеря всех сообщений на чистой установке; бесконечный flush-цикл перезаписывает Dexie каждую секунду |  |  |
| Kernel-сервисы диалогов | 5.5 | Движок резолвит кэш-попы как пустые ответы, игнорирует уже-aborted сигнал; индекс сообщений не привязан к сессиям |  |  |
| LLM-слой (адаптеры/HTTP) | 5 | SSE-парсер образцовый, но tool_calls в camelCase, обрыв стримов >120с, отмена Groq трактуется как сбой |  |  |
| Конфигурация (прокси/CSP) | 4.5 | 10+ провайдеров указывают на несуществующие /proxy/* роуты (404/403), OpenAI теряет /v1, Ollama блокируется CSP |  |  |
| Безопасность (XSS/инъекции) | 9 | Уязвимостей не найдено: DOMPurify на всех путях, allowlist протоколов, sandbox-iframe без same-origin, eval/Function в родителе отсутствуют |  |  |
| Тесты и типы | 4 | store.test.ts 36/36 зелёные, но 10/10 рендер-тестов ChatPanel мертвы (устаревший mock), 2391 ошибок typecheck (~122 в чат-зоне) |  |  |
| i18n чата | 6.5 | RU/EN полные для основных ключей, но ~34 захардкоженных EN-строки в CodeRunner/VoiceButton/ChatPanel, сломанный '{ms}' и 'Yesterday Prefix' |  |  |
| Средняя по слоям | 5.78 | Среднее арифметическое; интегральная оценка здоровья чата с весами критичности — 5.6/10 |  |  |
| Топ-5 рисков |  |  |  |  |
| № | ID | Риск |  | Последствие |
| 1 | C-03 | Первый запуск приложения — «чёрная дыра»: сообщения не отображаются и не сохраняются (фантомная сессия 'default') |  | Критический UX + потеря данных |
| 2 | C-01/C-02 | DeepSeek, Kimi, MiniMax, Qwen, Together, Fireworks, BlackBox, Scaleway, CometAPI, GitHub — мертвы (нет прокси-роутов); OpenAI — 404 (потерян /v1) |  | Основные провайдеры недоступны из коробки |
| 3 | H-06/H-05 | Стримы длиннее 120 секунд принудительно обрываются; отмена на Groq карает ключ и перезапускает запрос на другом провайдере |  | Прямой расход токенов против воли пользователя |
| 4 | C-04 | После синка OpenRouter цены попадают в калькулятор в неверных единицах — стоимость занижена в ~1 000 000 раз |  | Бюджеты и аналитика недостоверны |
| 5 | H-08 | Бесконечный цикл перезаписи Dexie каждую секунду в каждом открытом табе |  | Износ IndexedDB, расход батареи/CPU |
| • | SSE-парсер (src/llm/http/sse-parser.ts) образцово обрабатывает разрывы буфера, CRLF, multi-line data, [DONE] и keep-alive |  |  |  |
| • | XSS-защита образцовая: DOMPurify на всех путях рендера, allowlist http/https/mailto/tel, iframe sandbox без same-origin, отказ от eval/Function |  |  |  |
| • | Декоратор retry отказывается переигрывать стримы с уже отданными чанками; circuit breaker исключает пользовательские аборты |  |  |  |
| • | store.test.ts — 36/36 зелёных; ядро стора аккуратно покрыто |  |  |  |
| • | Грамотные FIX-комментарии (chat-partial, chat-persistence, chat-agent) показывают зрелый процесс починки регрессий |  |  |  |
| Полный перечень — лист «Находки»; план работ — лист «Роадмап»; факты проверки — лист «Тесты и метрики». |  |  |  |  |

## Находки

| Реестр находок аудита чата (45 позиций, верифицированы по исходникам) |  |  |  |  |  |  |
|---|---|---|---|---|---|---|
| ID | Серьёзность | Слой | Файл(ы) : строки | Заголовок | Сценарий и последствия | Рекомендация по исправлению |
| C-01 | Критический | Конфигурация | src/llm/registry/adapter-factory.ts:126-204; src/llm/openai-compatible/openai-compatible-adapter.ts:56; vite.config.ts:118-175; docker/nginx.conf:73-160 | 10+ провайдеров шлют трафик на несуществующие /proxy/* роуты | useProxy=true подменяет базовый URL на /proxy/<id>, но роуты описаны только для gemini/openrouter/nvidia/groq/cerebras/cloudflare/openai. DeepSeek, Kimi, MiniMax, Qwen, Together, Fireworks, BlackBox, Scaleway, CometAPI, GitHub получают 404 (dev, SPA-fallback) или 403 (prod, nginx catch-all deny all). Провайдеры включены в provider-default-models.ts и видны в UI. | Либо добавить явные /proxy/<id> роуты в vite.config.ts и nginx.conf (по образцу groq), либо передавать useProxy=false и добавить хосты провайдеров в CSP connect-src. |
| C-02 | Критический | Конфигурация | src/llm/registry/adapter-factory.ts:123-125; vite.config.ts:156-161; docker/entrypoint.sh:19 | OpenAI теряет префикс /v1 при проксировании — все запросы 404 | Адаптер создаётся с baseUrl https://api.openai.com/v1, но useProxy отбрасывает его: /proxy/openai/chat/completions → https://api.openai.com/chat/completions (без /v1) → 404 и в dev, и в prod (PROXY_OPENAI=https://api.openai.com). Groq/Cerebras работают лишь потому, что их прокси-таргеты содержат /v1. | Добавить /v1 в прокси-таргет (https://api.openai.com/v1) либо композиционировать версионированный путь в адаптере. |
| C-03 | Критический | Персистентность | src/stores/chat/hydration.ts:140-151; src/stores/chat/store-helpers.ts:14-24; src/stores/chat/chat-send-message.ts:234-266 | Фантомная сессия 'default': на чистой установке все сообщения молча теряются | При пустом Dexie liveQuery отдаёт sessions=[], но activeSessionId откатывается на DEFAULT_SESSION.id ('default'), которого в списке нет. updateSessionInList возвращает массив без изменений (id не найден): write-through syncSessions пишет пустоту, set() в sendMessage — no-op, STREAM_CHUNK/END — no-op. Пользователь печатает, спиннер крутится, ничего не появляется и не сохраняется. 'default' намеренно не персистится (session-manager-service.ts:373). | При пустом cleaned — сеять sessions:[DEFAULT_SESSION] (или сразу создавать персистентную сессию); в updateSessionInList при отсутствии активной сессии — добавлять её в список. |
| C-04 | Критический | LLM-слой | src/kernel/services/pricing-service.ts:279-289 против 168-173 и 8-41 | Цены per-token попадают в калькулятор per-1M — стоимость ошибается в 10^6 раз | syncFromOpenRouter() пишет parseFloat(p.prompt) (OpenRouter отдаёт цену за токен, например 0.00000225), а calculateCost считает (tokens/1_000_000)*price — расчёт рассчитан на цену за 1M токенов (как FALLBACK_PRICING). После первого синка все синхронизированные модели стоят в ~1 000 000 раз дешевле; смешанная таблица делает префикс-матч непоследовательным. | Умножать распарсенные значения на 1e6 при записи в pricingData (или хранить per-token и убрать делитель в calculateCost). Покрыть тестом единиц измерения. |
| H-01 | Высокий | Kernel-сервисы | src/kernel/services/conversation-execution-engine.ts:101-112; src/kernel/services/chat-executor.ts:804-815, 311-330 | Кэш-поп в движке диалогов резолвится как успешный пустой ответ | emitStatus(req,'cached') шлёт MESSAGE_RESPONSE с content:'' ДО реального ответа status:'done'. Движок резолвит промис на первом подходящем MESSAGE_RESPONSE и отписывается — до 'done' не доживает: {success:true, content:''}. CacheService подключён всегда (phase6-high-level.ts:115, TTL 5 мин, персистентный): повтор сценария с тем же первым промптом детерминированно даёт пустые ходы в истории диалога. | Резолвить только на терминальных статусах (done/error/cancelled/timeout) либо не переиспользовать канал MESSAGE_RESPONSE для промежуточных статусов. |
| H-02 | Высокий | Kernel-сервисы | src/kernel/services/conversation-execution-engine.ts:121-135 | Уже-aborted сигнал сессии игнорируется — отмена не останавливает LLM-вызов | addEventListener('abort') не срабатывает, если сигнал уже aborted. Достижимо: director создаёт контроллер заранее, abort() во время await policy.proposeNextTurn() оставляет сигнал aborted до того, как execute() повесит слушатель (пост-проверки после await в processNextStep нет). LLM-вызов доигрывается до конца (реальный расход), результат записывается как нормальный ход «отменённого» запуска. | В начале execute() проверять sessionSignal.aborted и сразу резолвить {success:false, error:'Aborted'}; после каждого await в processNextStep — повторная проверка aborted. |
| H-03 | Высокий | Kernel-сервисы | src/kernel/services/message-index-service.ts:133-178; src/stores/chat/chat-send-message.ts:272-287 | Индекс сообщений: всё помечается sessionId='unknown' — поиск по сессии мёртв | setCurrentSession() не вызывается ни разу в кодовой базе, эмиттеры SEND_MESSAGE не передают sessionId. Каждое проиндексированное сообщение получает sessionId:'unknown': фильтр по сессии в search() и uniqueSessions() — мёртвый код, CHAT_REWOUND-чистка никогда не совпадает. | Прокидывать sessionId в payload SEND_MESSAGE и штамповать обе записи (user+assistant) из буфера запроса при STREAM_END. |
| H-04 | Высокий | LLM-слой | src/llm/core/base-adapter.ts:35-42 | История tool-вызовов сериализуется в camelCase (toolCalls) вместо wire-формата tool_calls | OpenAI-совместимый wire-формат требует tool_calls/tool_call_id. Затрагивает OpenRouter (mapMessages:true) и NVIDIA, а через passthrough-ветку — OpenAI-compatible/DeepSeek/Cloudflare. role:'tool' без tool_call_id → 400 у строгих провайдеров; история tool-вызовов невидима у снисходительных — ломаются многоходовые tool-циклы. | Мэпить в tool_calls/tool_call_id в обеих ветках buildRequestBody (эталон — groq-adapter.ts:58-63). |
| H-05 | Высокий | LLM-слой | src/llm/groq/groq-adapter.ts:193-205; src/kernel/services/chat-executor.ts:577-598 | Отмена пользователем на Groq классифицируется как сбой провайдера и переотправляется | groq-sdk превращает AbortError в APIUserAbortError (обычный Error). Проверка executor'а `error instanceof DOMException && name==='AbortError'` не срабатывает → handleProviderError штрафует ключ, провайдер исключается и отменённый запрос выполняется повторно на фолбэке — противоположность отмене, плюс ложная деградация здоровья ключа. | В normalizeError распознавать name==='APIUserAbortError' (или пробрасывать DOMException); в executor'е трактовать «аборт при signal.aborted» как отмену. |
| H-06 | Высокий | LLM-слой | src/llm/http/llm-http-client.ts:133-151, 377-453 | Таймер таймаута 120с не очищается для стримов — длинные ответы обрываются | streamPost возвращает Response в finally без clearTimeout: таймер #withTimeout остаётся заряженным и абортит fetch-сигнал посреди тела на PROVIDER_HTTP_TIMEOUT_MS (120с). Любая генерация дольше 2 минут (длинные ответы, reasoning-модели) гибнет с TimeoutError → LLMError 408 → executor трактует как сбой провайдера, штрафует ключ и уходит в фолбэк — ответ потерян целиком. | Очищать таймер при получении заголовков в streamPost (или применять таймаут только до первого байта). |
| H-07 | Высокий | LLM-слой | src/llm/groq/groq-adapter.ts:125-134 | Groq-стрим выбрасывает finish_reason, usage и tool_calls — бюджет считает $0 | Единственный адаптер без терминального onChunk('', {finishReason, tokens}). llm-client-service падает на оценку content.length/4, chat-executor пишет tokens: result.tokens \|\| 0 → STREAM_END → BudgetService считает $0 для каждого Groq-стрима; обрезка finish_reason:'length' невидима пользователю. | Накапливать finish_reason / usage / delta.tool_calls и эмитить терминальный мета-чанк (эталон — openai-compatible-adapter.ts:159-165). |
| H-08 | Высокий | Персистентность | src/stores/chat/hydration.ts:47-69, 200-209 | Бесконечный flush-цикл: полная перезапись Dexie каждую секунду | finally-блок flush всегда аллоцирует новый Set → subscribe срабатывает на каждом завершении flush; epoch-скип не применяется (lastFlushEpoch выровнен в начале flush), и timer перепланируется — вечно, даже без изменений. Каждый цикл переписывает до 100 сессий (get+put в транзакции), бесконечно раздувая version и сжигая IO/CPU во всех табах. | setState только если deletedIds реально сократился; скипать flush, если сериализованный payload не изменился. |
| H-09 | Высокий | Store | src/stores/chat/chat-send-message.ts:78-86, 306-319; src/components/ChatPanel/ResponseCard.tsx:229-238 | Очередь отправки выбрасывает сообщения во время стрима первой отправки | Первый sendMessage резолвится сразу после эмита SEND_MESSAGE (стрим идёт секунды). finally декврует следующее сообщение, но глобальный isAnySending() ещё true → тост «Cannot send» и выход — декврованный текст НЕ отправлен, НЕ возвращён в очередь и НЕ восстановлен. Достижимо из UI: кнопки regenerate/retry не задизейблены во время стрима. Отправка во вторую сессию во время стрима также блокируется глобально. | Ре-очередь сообщения до опустошения activeRequestIds (или отложенный dequeue по терминальному событию). |
| H-10 | Высокий | Store | src/stores/chat/chat-event-handlers.ts:112-127; src/components/ChatPanel/ChatPanel.tsx:56 | Обновление стора на каждый токен — полный ре-рендер панели на каждый чанк | STREAM_CHUNK форвардится per-SSE-событие без rAF/временного батчинга: каждый чанк пересобирает массивы sessions/history/responses и триггерит всех подписчиков, включая ChatPanel (селектор на sessions). При длинном стриме — сотни полных ре-рендеров панели (хедер, сайдбар, агент-селектор) в секунду. | Коалесцировать чанки через requestAnimationFrame (16-25мс) перед set(); сузить подписки ChatPanel. |
| H-11 | Высокий | UI | src/components/ChatPanel/ChatMessagesSection.tsx:54-59, 107-118, 80-83 | Виртуализатор с фиксированной высотой 120px без измерения — наложения и сломанный автоскролл | Строки позиционируются абсолютно с оценочной высотой (estimateSize:()=>120), measureElement не используется. Любое сообщение выше 120px (то есть почти любой ответ ассистента) налезает на следующую строку, getTotalSize() неверен, scrollToIndex по оценкам промахивается, а как только реальный контент превысит оценочный низ на 100px, userScrolledUpRef ложноположительно срабатывает — автоскролл стрима останавливается без участия пользователя. | Рендерить через virtualizer.measureElement (динамическое измерение) и якорить автоскролл по нижнему элементу. |
| H-12 | Высокий | Тесты | src/components/ChatPanel/ChatPanel.test.tsx (mock ../../kernel/instances) | 10/10 рендер-тестов флагманской панели мертвы: устаревший mock без rootLogger | Модуль kernel/instances обзавёлся экспортом rootLogger, но vi.mock в тесте не обновлён — каждый тест падает с «No rootLogger export is defined on the mock». Рендер-покрытие ChatPanel фактически нулевое при «зелёном» CI-виде (vitest считает файл упавшим, но в выборке chat-zone его легко не заметить). | Дополнить vi.mock экспортом rootLogger (или importOriginal); добавить CI-гейт на падающие чат-тесты. |
| H-13 | Высокий | Тесты/типы | tsc -b --noEmit: 2391 ошибок всего; src/stores/chat/types.ts:105-107 и др. | Typecheck красный: 2391 ошибок по проекту, ~122 в чат-зоне | npm run build существует в варианте build:skip-typecheck с WARN-сообщением «broken types will NOT fail this build» — типы не являются гейтом. В чат-зоне: async-методы стора объявлены как void (types.ts:105-107), ChatPanel вызывает .then/.catch на «void» (436, 460, 487-502), ChatDock/PersonaSelector — битые/неиспользуемые импорты. Runtime это не роняет (реально возвращается Promise), но контракт типов лжив и маскирует реальные ошибки. | Исправить сигнатуры (Promise<void>) и довести чат-зону до 0 ошибок tsc; включить typecheck в CI-гейт. |
| M-01 | Средний | Kernel-сервисы | src/kernel/services/message-index-service.ts:95-103 | CHAT_REWOUND никогда не эмитится; хендлер сносит весь индекс сессии | Событие chat:rewound не эмитится нигде — ветка мёртвая, инвалидация индекса при rewind не работает. Контракт payload ({sessionId, messageId, truncatedCount}) описывает усечение хвоста, а хендлер удаляет ВСЕ сообщения сессии — ждёт первого эмиттера, чтобы разойтись с источником истины. | Эмитить CHAT_REWOUND из пути rewind (или удалить мёртвую подписку); удалять только усечённый хвост. |
| M-02 | Средний | Kernel-сервисы | src/kernel/services/session-manager-service.ts:381-383, 418-421 | getDebateHistory возвращает пустоту во время загрузки; при ошибке история не загрузится никогда | ensureHistoryLoaded() ставит _historyLoaded=true ДО await стора; результат загрузки никем не оповещается. DebateHistoryPage и DebatePanel читают синхронно на монтировании → пустая история до случайного debate:updated. Если loadHistoryList упадёт — ошибка проглочена, флаг остаётся true, ретрая загрузки нет до перезапуска приложения. | Сделать getDebateHistory асинхронным (или эмитить уведомление после начальной загрузки) и сбрасывать флаг при ошибке. |
| M-03 | Средний | Kernel-сервисы | src/kernel/services/session-manager-service.ts:414-421; src/kernel/services/debate-runtime/debate-session-persistence.ts:46-81 | Круговой путь персистенции дебат-истории стирает tags/folder/isArchived | persistHistoryList → sessionToRecord() не переносит tags/folder/isArchived в DebateSessionRecord, а saveSnapshot делает full put-замену. Каждая запись истории (новый дебат, restore, delete, clear — переписывает ВСЕ записи) стирает теги/папки и воскрешает архивированные дебаты. | Прокидывать tags/folder/isArchived через recordToSession/sessionToRecord либо merge-сохранять неизвестные поля записи в saveSnapshot. |
| M-04 | Средний | Kernel-сервисы | src/kernel/services/session-affinity-store.ts:39-55, 105-125; session-manager-service.ts:324 | Аффинити-биндинги: reap не персистится, удаление сессии не развязывает — рост навсегда | reapExpired() удаляет биндинги из памяти, но persistBindings() не вызывает — после перезагрузки «призрачные» биндинги воскрешаются. SESSION_DELETED никто не обрабатывает, а router-ranking биндит каждый чат-сеанс; здоровые биндинги без TTL — карта и JSON в Dexie растут месяцами. | Персистить после reap; подписаться на SESSION_DELETED (или добавить age-TTL) с персистенцией удаления. |
| M-05 | Средний | Kernel-сервисы | src/kernel/services/chat-bookmarks-service.ts:54-83, 153-174 | Закладки: CAS-циклы сдаются молча — потерянные/призрачные закладки | storage.save/delete после 3 неудач CAS не бросают исключение: addBookmark кэширует и эмитит CHAT_BOOKMARK_ADDED для незаписанной закладки (пропадёт после перезагрузки), removeBookmark удаляет из кэша при живой записи в БД (воскреснет). Двойной клик проходит дедуп до записи — две закладки на один message. | Бросать исключение после исчерпания попыток; ставить in-flight-маркер в кэш до await. |
| M-06 | Средний | Store | src/stores/chat/store.ts:19-28, 203-207, 252, 562-603, 655 | clearHistory/switchModel/switchKey/setAgent/editEntry пишут в переключённую сессию | updateActiveSession резолвит сессию в момент вызова, но вызовы происходят после await (persist/lock). Если пользователь переключил сессию во время await clearHistory — uas(()=>[]) сносит историю УЖЕ ДРУГОЙ активной сессии, и flush через 1с персистит потерю. Для switchModel/setAgent/editEntry системная запись/правка попадает в чужую сессию. | Захватывать sessionId до первого await и патчить конкретную сессию через updateSessionInList, минуя uas. |
| M-07 | Средний | UI | src/components/ChatPanel/ChatInputArea.tsx:43-57; src/components/ChatPanel/ChatPanel.tsx:203-216 | Текст ввода уничтожается до отправки; ошибка persist теряет сообщение навсегда | handleSend очищает input ДО await onSend (sendMessage пробрасывает ошибку при сбое write-through). handleSend не awaiting и не catch → потеря текста пользователя + unhandled promise rejection при каждом сбое Dexie. | catch в handleSend с восстановлением input (или очищать только после принятия записи стором). |
| M-08 | Средний | Store | src/stores/chat/store.ts:263-274; src/components/ChatPanel/ChatPanel.tsx:274-284 | createSession при ошибке возвращает '' — панель переключается на несуществующую сессию | createSession глотает ошибку и возвращает '', поэтому try/catch в handleNewChat никогда не срабатывает, а setActiveSessionId('') затирает персистентный chat_active_session_id и указывает активную сессию «в никуда» (дальше — фантомная сессия C-03). | Бросать исключение из createSession (или проверять if (newId) перед setActiveSessionId). |
| M-09 | Средний | Store | src/stores/chat/store.ts:75-88 | loadMoreSessions: offset по длине sessions (включая in-memory) навсегда пропускает сессии | offset = sessions.length, но в списке могут быть не-Dexie записи (in-memory 'default' после deleteSession, пре-гидрационные) — они сдвигают offset в Dexie, реальные строки пропускаются и никогда не догружаются (offset только растёт). | Держать отдельный счётчик загруженных из БД строк или пагинировать курсором по updatedAt. |
| M-10 | Средний | Store | src/stores/chat/chat-send-message.ts:237-266 | Write-through персистит несрезанную историю — расхождение памяти и Dexie | syncSessions получает history без .slice(-MAX_HISTORY), а in-memory set() срезает до 200. Dexie накапливает >200 записей, после перезагрузки «лишние» старые записи воскрешаются — два источника истины расходятся. | Применять одинаковый .slice(-MAX_HISTORY) перед syncSessions. |
| M-11 | Средний | Kernel-сервисы | src/kernel/services/chat-executor.ts:600-614 | 429-фолбэк без задержки: Retry-After игнорируется, провайдеры выжигаются мгновенно | Цикл переключения провайдеров при 429 не делает паузу (retryAfter из RetryableError не используется) — при аккаунтовом лимите все MAX_429_RETRIES провайдеров отстреливаются подряд за миллисекунды и все фейлятся. | Хонорить RetryableError.retryAfter задержкой перед re-dispatch. |
| M-12 | Средний | LLM-слой | src/llm/openai-compatible/openai-compatible-adapter.ts:129; src/llm/nvidia/nvidia-nim-adapter.ts:119; src/kernel/services/llm-client-service.ts:101-104 | Стримы не запрашивают usage (stream_options) — токены считаются по chars/4 | OpenAI и NVIDIA отдают usage в стриме только при stream_options:{include_usage:true} — флаг не ставится нигде. Фолбэк Math.ceil(content.length/4) занижает кириллицу ~в 2 раза (а русский — первоклассный язык продукта); промпт-токены не учитываются вовсе. Эти числа кормят recordUsage, tps, STREAM_END → BudgetService. | Ставить stream_options при stream=true (с guard для не поддерживающих провайдеров); использовать CJK/Cyrillic-aware estimateTokenCount и оценивать промпт-токены. |
| M-13 | Средний | LLM-слой | src/llm/openai-compatible/openai-compatible-adapter.ts:144-154; src/llm/nvidia/nvidia-nim-adapter.ts:119 | Дельты streamed tool_calls игнорируются — стрим с инструментами не собирает вызовы | Экстрактор читает только delta.content/reasoning_content: аккумуляции tool_calls по index нет, в терминальном мета их нет. Любой стрим с options.tools (OpenAI-compatible, DeepSeek, NVIDIA, Cloudflare) отдаёт контент без tool-вызовов, хотя non-streaming путь их поддерживает — поведение зависит от транспорта. | Аккумулировать delta.tool_calls по index и отдавать в терминальном мета (StreamMeta.toolCalls). |
| M-14 | Средний | LLM-слой | src/llm/cloudflare/cloudflare-adapter.ts:82-85, 111, 118-119 | Cloudflare finish_reason не нормализован — отбрасывается валидатором | Адаптер эмитит сырые 'stop'/'length', а llm-client-service валидирует только UPPERCASE AdapterFinishReason ('STOP','MAX_TOKENS'...) — значение всегда становится undefined, обрезка length невидима. Non-streaming путь вообще не возвращает finishReason. Все прочие адаптеры прогоняют через normalizeFinishReason. | Прогонять терминальный мета через normalizeFinishReason и возвращать finishReason из doSendMessage. |
| M-15 | Средний | LLM-слой | src/llm/registry/adapter-factory.ts:239-244; src/llm/groq/groq-adapter.ts:44 | Амплификация ретраев: SDK maxRetries:2 внутри RetryDecorator maxRetries:3 | Groq SDK ретраит сам (2 попытки), будучи обёрнутым в RetryDecorator (3 попытки): один логический вызов при 5xx → до 12 upstream POST с каждым своим 120с таймаутом — минуты ожидания до фолбэка и риск повторного биллинга. | maxRetries:0 в SDK (или учитывать SDK-ретраи в декораторе). |
| M-16 | Средний | Конфигурация | src/llm/registry/adapter-factory.ts:211-221; vite.config.ts:114; docker/nginx.conf (CSP) | Ollama/LM Studio блокируются CSP — локальные провайдеры не работают из коробки | Адаптеры ходят напрямую на http://localhost:11434/1234 (useProxy=false), но CSP connect-src (dev и prod) не содержит localhost — браузер отказывает в соединении. Провайдеры рекламируются в UI как доступные. | Пустить через прокси либо добавить http://localhost:11434/1234 в dev-CSP и задокументировать prod-требование. |
| M-17 | Средний | UI/Kernel | src/kernel/services/conversation-director-service.ts:208; src/stores/directorController.ts:47; src/components/DirectorPanel/RunTab.tsx:131-216 | run() директора пробрасывает ошибки, UI их не ловит — unhandled rejections | run() rethrow'ит и при сбое хода, и при аборте; handleRun и onClick={controls.resume} без try/catch — каждый упавший/остановленный оператором запуск даёт unhandled promise rejection, сообщение об ошибке до пользователя не доходит (только бейдж статуса). | Обернуть controls.run()/controls.resume() в try/catch с показом e.message. |
| M-18 | Средний | Store | src/stores/chat/chat-send-message.ts:268-270; src/stores/chat/chat-event-handlers.ts (нет delete) | requestEntryMap растёт неограниченно — утечка за время жизни таба | Ссылки requestId→{sessionId, entryId} добавляются для каждого ответа каждого запроса и не удаляются на терминальных статусах (чистка только в deleteSession/forkSession) — за долгую сессию с сотнями сообщений карта живёт до перезагрузки таба. | Удалять записи на терминальных статусах в chat-event-handlers. |
| L-01 | Низкий | Персистентность | src/stores/chat/hydration.ts:216-234 | Аварийный backup хранит 5 СТАРЕЙШИХ сессий вместо свежих | sessions отсортированы по updatedAt по убыванию; slice(-5) берёт последние (старейшие) — против intent «keep recent» в fallback-пути >4.5МБ. | slice(0, 5). |
| L-02 | Низкий | UI/i18n | src/components/ChatPanel/ResponseCard.tsx:172-173, 321-322; src/i18n/translations/en/chat.ts:94 | Плейсхолдер {ms} не подставляется — в UI видно «1234{ms}ms» | t('chat.latency_ms') вызывается без params: EN-шаблон '{ms}ms' рендерится буквально после значения; RU-вариант даёт «1234Задержка, мс». | t('chat.latency_ms', { ms: res.latency }). |
| L-03 | Низкий | i18n | CodeRunner.tsx (~15), VoiceButton.tsx (9), ChatPanel.tsx (6), CopyButton.tsx (2), PersonaSelector.tsx (2); src/i18n/translations/en/chat.ts:142 | ~34 захардкоженных EN-строки в чат-UI + заглушка 'Yesterday Prefix' | 'Execute Code', 'Microphone access denied', 'Agent:', '— No agent (direct) —', 'Copy'/'Copied!' и др. мимо t(); ключ chat.yesterday_prefix в EN — непереведённый плейсхолдер, который formatTime рендерит как «Yesterday Prefix12:30». RU-тексты не хардкодятся (только комментарии). | Перевести все строки через t(); заполнить yesterday_prefix ('Yesterday at'/'Вчера в'). |
| L-04 | Низкий | UI | src/components/ChatPanel/CodeRunner.tsx:8, 284-303 | CodeRunner рекламирует ts/typescript, но исполняет код как JS — гарантированный SyntaxError | ts/typescript в EXECUTABLE_LANGS, но аннотации типов не вырезаются перед сборкой (async function(){...}) — любой TS-блок валит весь инлайн-скрипт (или упирается в 10с таймаут). | Убрать ts из allowlist или добавить транспиляцию (esbuild-wasm/sucrase) перед исполнением. |
| L-05 | Низкий | Kernel-сервисы | src/kernel/services/chat-summarizer-service.ts:251-256, 101 | manualSummarize требует порога авто-триггера — ручное суммирование короткого чата молча пусто | Делегирует generateSummary, возвращающий null при messages.length < triggerAfterMessages: пользователь явно просит сводку 10-сообщений — получает ничего, без объяснения. | Добавить force-флаг, обходящий порог минимума сообщений. |
| L-06 | Низкий | Kernel-сервисы | src/kernel/services/chat-summarizer-service.ts:329-368 | mergeSummaries не персистит и не дожидается загрузки | Единственный write-путь без #persist: merged-сводка только в памяти (потеря при перезагрузке); может молча перезаписать загруженную сводку при вызове до #ensureLoaded. | await #ensureLoaded() и void #persist() после merge. |
| L-07 | Низкий | Kernel-сервисы | src/kernel/services/persona-service.ts:159-223 | Повторная инициализация персоны накапливает stale-записи | init() не очищает this.personas: задокументированный путь setDatabase → isInitialized=false → init() мержит кастомные персоны новой БД поверх старых — «призраки» остаются доступными для выбора в чате. | Очищать this.personas (или удалять не-builtin) в начале init(). |
| L-08 | Низкий | Персистентность | src/kernel/dal/session-repository.ts:13-42, 63-67 | DAL-репозиторий кэширует живую ссылку и не инвалидируется | save() кладёт в кэш оригинал объекта (не копию), а параллельные записи той же таблицы через DexieSessionStore (с version CAS) кэш не инвалидируют — getAll()/get() могут вечно отдавать устаревшие сессии. Сейчас репозиторий потребляется только тестами. | Копировать при save() и инвалидировать при write-through из DexieSessionStore (или убрать кэш). |
| L-09 | Низкий | UI/Store | src/kernel/services/chat-executor.ts:472-473; src/components/ChatPanel/ChatHistoryEntry.tsx:283 | id ответа меняется на терминальном событии — remount ResponseCard в момент завершения | Терминальный MESSAGE_RESPONSE приносит новый id, merge {...r, ...res} подменяет id стрим-заглушки → React key меняется → карточка размонтируется/монтируется ровно при завершении ответа (фликер; локальные copied/feedback сбрасываются). | Исключить id из merge (или ключовать по requestId). |
| L-10 | Низкий | Kernel-сервисы | src/kernel/services/conversation-director-service.ts:323-327; session-manager-service.ts:429; message-index-service.ts:155-187 | Мелкие дефекты согласованности: аборты в failed-счётчике, мёртвое условие, probe в индексе | 1) Абортированные ходы увеличивают failed-счётчик запуска (это отмена, не сбой). 2) Условие _pendingHistorySaves.length >= 0 всегда истинно — маскирует намерение. 3) STREAM_END от key-проб индексируется как чат-сообщения (загрязняет MessageSearchPanel), а пользовательское сообщение при ошибке/отмене не индексируется вовсе. | Флаг aborted в событии; убрать мёртвый операнд; dedicated-событие для probe или маркер source='probe'. |

## Роадмап

| Роадмап исправлений — 6 фаз, 35 задач |  |  |  |  |  |  |  |
|---|---|---|---|---|---|---|---|
| ID | Фаза | Задача | Файлы | Усилие | Приоритет | Критерий приёмки | Закрывает |
| Фаза 0 — Hotfix (48 часов) |  |  |  |  |  |  |  |
| R-01 | Ф0 | Оживить фантомную сессию: сеять DEFAULT_SESSION при пустом Dexie, append при отсутствии активной сессии | hydration.ts, store-helpers.ts | S | P0 | На чистой установке первое сообщение отправляется, отображается и переживает перезагрузку страницы | C-03 |
| R-02 | Ф0 | Остановить flush-цикл: setState только при фактическом сокращении deletedIds + дифф-чек payload | hydration.ts | S | P0 | В простое (без действий пользователя) IndexedDB не получает записей в течение 30с | H-08 |
| R-03 | Ф0 | Добавить /v1 в прокси-таргет OpenAI (dev и prod) | vite.config.ts, docker/entrypoint.sh, docker/nginx.conf | S | P0 | Запрос с ключом OpenAI через /proxy/openai/chat/completions возвращает 200 | C-02 |
| R-04 | Ф0 | Провайдерам без прокси-роутов выключить useProxy и расширить CSP (или добавить роуты по образцу groq) | adapter-factory.ts, vite.config.ts, docker/nginx.conf | M | P0 | DeepSeek/Kimi/MiniMax/Qwen/Together/Fireworks отвечают 200 с валидным ключом; в smoke-чеклисте нет 404/403 | C-01 |
| R-05 | Ф0 | Нормализовать единицы цен из OpenRouter (×1e6 при инжестe) + юнит-тест | pricing-service.ts | S | P0 | После syncFromOpenRouter стоимость gpt-4o-mini на 1M токенов совпадает с прайсом OpenRouter ±5% | C-04 |
| Фаза 1 — Стриминг и отмена (1-2 недели) |  |  |  |  |  |  |  |
| R-06 | Ф1 | Очищать таймаут-таймер после получения заголовков в streamPost | llm-http-client.ts | S | P1 | Интеграционный тест: стрим живёт 150с без TimeoutError | H-06 |
| R-07 | Ф1 | Корректная классификация отмены: APIUserAbortError + проверка signal.aborted в executor | groq-adapter.ts, chat-executor.ts | M | P1 | Cancel на Groq даёт статус cancelled, ключ не штрафуется, фолбэк-запрос не отправляется | H-05 |
| R-08 | Ф1 | Wire-формат tool_calls/tool_call_id в base-adapter (обе ветки) | base-adapter.ts, groq-adapter.ts (эталон) | S | P1 | Многоходовой tool-цикл проходит на OpenRouter и NVIDIA без 400 | H-04 |
| R-09 | Ф1 | Стримы: stream_options.include_usage, аккумуляция delta.tool_calls, finish_reason у Groq и Cloudflare | openai-compatible-adapter.ts, nvidia-nim-adapter.ts, groq-adapter.ts, cloudflare-adapter.ts | M | P1 | STREAM_END несёт реальные usage-токены для OpenAI/NVIDIA/Groq; finish_reason:'length' показывается пользователю | M-12, M-13, H-07, M-14 |
| R-10 | Ф1 | Коалесцировать STREAM_CHUNK через requestAnimationFrame (16-25мс) перед set() | chat-event-handlers.ts | M | P1 | React DevTools Profiler: ≤60 ре-рендеров ChatPanel в секунду при непрерывном стриме | H-10 |
| R-11 | Ф1 | Очередь отправки: ре-очередь вместо выброса, dequeue после опустошения activeRequestIds | chat-send-message.ts | M | P1 | Регресс-тест: 2 быстрые отправки — обе доставлены, ни одна не потеряна; обновить store.test.ts:337 | H-09 |
| R-12 | Ф1 | 429-фолбэк: задержка по Retry-After перед re-dispatch | chat-executor.ts | S | P1 | Мок 429 c Retry-After: между попытками выдержана пауза | M-11 |
| Фаза 2 — Данные и согласованность (1-2 недели) |  |  |  |  |  |  |  |
| R-13 | Ф2 | Движок диалогов: резолв только на терминальных статусах + early-exit при уже-aborted | conversation-execution-engine.ts | S | P1 | Кэш-поп возвращает полный cached.response; abort во время policy-await не отправляет LLM-запрос | H-01, H-02 |
| R-14 | Ф2 | Прокинуть sessionId в SEND_MESSAGE; штамповать записи индекса из буфера запроса | chat-send-message.ts, message-index-service.ts | M | P1 | Поиск по сессии в MessageSearchPanel возвращает только сообщения выбранной сессии | H-03, M-01 |
| R-15 | Ф2 | Захватывать sessionId до первого await в clearHistory/switchModel/switchKey/setAgent/editEntry | store.ts | M | P1 | Регресс-тест: переключение сессии во время clearHistory не трогает новую сессию | M-06 |
| R-16 | Ф2 | Закладки: throw после исчерпания CAS + in-flight дедуп до await | chat-bookmarks-service.ts | S | P1 | Сбой CAS приводит к уведомлению, а не к фантомной закладке; двойной клик создаёт одну закладку | M-05 |
| R-17 | Ф2 | Аффинити: персистить reap; обрабатывать SESSION_DELETED (или age-TTL) | session-affinity-store.ts | S | P2 | После перезагрузки истёкшие/удалённые биндинги не воскрешаются; размер JSON ограничен | M-04 |
| R-18 | Ф2 | Пагинация: отдельный счётчик загруженных из БД строк вместо sessions.length | store.ts | S | P2 | Load More догружает непрерывный список без пропусков при наличии in-memory default | M-09 |
| R-19 | Ф2 | Единый .slice(-MAX_HISTORY) для write-through и памяти | chat-send-message.ts | S | P2 | После 210 сообщений Dexie и память содержат одинаковые 200 записей | M-10 |
| R-20 | Ф2 | Чистить requestEntryMap на терминальных статусах; исключить id из merge ответа | chat-event-handlers.ts, chat-executor.ts | S | P2 | Размер requestEntryMap стабилен; ResponseCard не ремантируется при завершении ответа | M-18, L-09 |
| Фаза 3 — UI/UX (1 неделя) |  |  |  |  |  |  |  |
| R-21 | Ф3 | Виртуализатор: measureElement + динамические высоты + якорный автоскролл | ChatMessagesSection.tsx | M | P1 | Длинные ответы не накладываются; автоскролл стрима не отваливается без действий пользователя | H-11 |
| R-22 | Ф3 | Ввод: await onSend + восстановление текста при ошибке отправки | ChatInputArea.tsx, ChatPanel.tsx | S | P2 | При принудительном сбое Dexie текст возвращается в поле, unhandled rejection исчезает | M-07 |
| R-23 | Ф3 | Guard: createSession бросает ошибку / handleNewChat проверяет пустой id | store.ts, ChatPanel.tsx | S | P2 | Сбой создания сессии оставляет активную сессию прежней и показывает уведомление | M-08 |
| R-24 | Ф3 | Подставить {ms}: t('chat.latency_ms', { ms: res.latency }) | ResponseCard.tsx | S | P3 | В EN отображается «1234ms», в RU — осмысленная строка без сырого плейсхолдера | L-02 |
| R-25 | Ф3 | Локализовать ~34 захардкоженные строки; заполнить yesterday_prefix | CodeRunner.tsx, VoiceButton.tsx, ChatPanel.tsx, CopyButton.tsx, PersonaSelector.tsx, translations/en+ru | M | P2 | grep-гейт: в перечисленных файлах 0 строковых литералов в JSX вне t(); переключение на RU переводит весь чат | L-03 |
| R-26 | Ф3 | CodeRunner: убрать ts/typescript из allowlist либо добавить транспиляцию | CodeRunner.tsx | S | P3 | TS-блок либо исполняется корректно, либо кнопка Run для него не показывается | L-04 |
| Фаза 4 — Тесты и типы (1 неделя) |  |  |  |  |  |  |  |
| R-27 | Ф4 | Починить vi.mock kernel/instances (добавить rootLogger) — оживить 10 тестов ChatPanel | ChatPanel.test.tsx | S | P1 | npx vitest run src/components/ChatPanel — 10/10 зелёные | H-12 |
| R-28 | Ф4 | Регресс-тесты на критические находки: фантомная сессия, flush-цикл, кэш-поп движка, единицы цен, proxy-роуты | stores/chat, kernel/services, llm (новые сьюты) | M | P1 | Каждая из C-01..C-04 и H-01/H-08 покрыта падающим-без-фикса тестом | C-01..C-04, H-01, H-08 |
| R-29 | Ф4 | Типы стора: async-методы → Promise<void>; довести чат-зону до 0 ошибок tsc | stores/chat/types.ts, ChatPanel.tsx, ChatDock.tsx | M | P2 | tsc --noEmit (tsconfig.app) не даёт ошибок в src/stores/chat, src/components/ChatPanel*, src/llm | H-13 |
| R-30 | Ф4 | e2e happy-path чата: чистый запуск → отправка → стрим → перезагрузка → данные на месте | e2e/ (playwright) | L | P2 | Playwright-тест зелёный в CI; фиксирует C-03 и связку hydration+persist | C-03 |
| Фаза 5 — Надёжность сервисов (по мере сил) |  |  |  |  |  |  |  |
| R-31 | Ф5 | Суммаризатор: force-флаг для manualSummarize; persist+ensureLoaded в mergeSummaries | chat-summarizer-service.ts | S | P3 | Ручная сводка на 10 сообщениях работает; merged-сводка переживает перезагрузку | L-05, L-06 |
| R-32 | Ф5 | persona-service: очистка non-builtin персон при повторном init | persona-service.ts | S | P3 | setDatabase на другую БД не оставляет персон из старой | L-07 |
| R-33 | Ф5 | Сохранять tags/folder/isArchived при персистенции дебат-истории | debate-session-persistence.ts, session-manager-service.ts | M | P3 | Архив/теги/папка переживают любую перезапись истории | M-03 |
| R-34 | Ф5 | Director: try/catch в RunTab, abort не считать failed; убрать мёртвое условие >=0 | RunTab.tsx, directorController.ts, conversation-director-service.ts, session-manager-service.ts | S | P3 | Остановка запуска оператором не растит failed; ошибочные запуски показывают сообщение | M-17, L-10 |
| R-35 | Ф5 | Гигиена: CSP для localhost (Ollama/LM Studio), удалить битый импорт PersonaSelector, проводка CHAT_REWOUND | vite.config.ts, nginx.conf, ChatPanel/PersonaSelector.tsx, message-index-service.ts | S | P3 | Локальные провайдеры подключаются; grep не находит ссылок на удалённый модуль; rewind чистит индекс корректно | M-16, M-01 |
| Усилие: S ≈ 0,5–1 день · M ≈ 2–4 дня · L ≈ 1 неделя. Оценки для одного разработчика, знакомого с кодовой базой. |  |  |  |  |  |  |  |

## Тесты и метрики

| Факты инструментальной проверки |  |  |
|---|---|---|
| Проверка / сьют | Результат и детали |  |
| typecheck (tsc -b --noEmit) | 2391 ошибок по проекту; в чат-зоне (ChatPanel/ChatDock/PersonaSelector/stores/chat/llm) — 122. Сборка имеет режим build:skip-typecheck с предупреждением. |  |
| vitest (чатовые сьюты, 10 файлов) | 158 теста: 127 passed / 24 failed / 1 skipped / 2 ошибки воркера. |  |
| store.test.ts | 36/36 зелёные — ядро стора покрыто хорошо (но тест 337 фиксирует потерю сообщения из очереди как ожидаемое поведение). |  |
| ChatPanel.test.tsx | 10/10 ПАДАЮТ: устаревший vi.mock ../../kernel/instances без экспорта rootLogger. |  |
| chat-executor.test.ts / ChatService.test.ts | 4 падения: auto-routing (no providers), cache-hit, STREAM_START/END, destroy(). |  |
| conversation-execution-engine.test.ts | 5/5 зелёные, но мокирует executor — поэтому H-01/H-02 не ловятся. |  |
| Покрытие XSS-поверхности | Уязвимостей не найдено: DOMPurify на всех путях, allowlist протоколов, sandbox-iframe без same-origin. |  |
| Состав фаз роадмапа |  |  |
| Фаза | Задач | Фокус |
| Фаза 0 — Hotfix (48 часов) | 5 | Блокеры первого запуска: фантомная сессия, flush-цикл, прокси OpenAI и 10 провайдеров, единицы цен |
| Фаза 1 — Стриминг и отмена (1-2 недели) | 7 | Надёжность LLM-конвейера: таймауты, отмена, tool_calls, usage, батчинг чанков, очередь, 429-бэкофф |
| Фаза 2 — Данные и согласованность (1-2 недели) | 8 | Движок диалогов, индекс сообщений, гонки сессий, закладки, аффинити, пагинация, утечки |
| Фаза 3 — UI/UX (1 неделя) | 6 | Виртуализатор, восстановление ввода, i18n чата, CodeRunner |
| Фаза 4 — Тесты и типы (1 неделя) | 4 | Оживление тестов ChatPanel, регресс-тесты критических находок, типы, e2e |
| Фаза 5 — Надёжность сервисов (по мере сил) | 5 | Суммаризатор, персоны, дебат-история, директор, гигиена конфигов |
| Всего задач | 35 |  |

## Review

| Перекрёстные проверки (живые формулы) |  |  |  |
|---|---|---|---|
| Проверка | Ожидание | Факт | Статус |
| Всего находок (Сводка = Находки) | 45 | 45 | PASS |
| Критических | 4 | 4 | PASS |
| Высоких | 13 | 13 | PASS |
| Средних | 18 | 18 | PASS |
| Низких | 10 | 10 | PASS |
| Задач роадмапа | 35 | 35 | PASS |
| Слоёв оценено | 9 | 9 | PASS |
| Сумма задач по фазам (Тесты и метрики) | 35 | 35 | PASS |
