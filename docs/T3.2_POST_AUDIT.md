# T3.2 POST Audit — Карточка агента (профиль + действия)

> Дата: 2026-09-09. Scope: новый таб Profile в detail-модалке. Без новых роутов/сторов.

## Что сделано
1. **`AgentProfileTab.tsx`** (новый): read-only карточка —
   - Characteristics: role, status, temperature, tools, skills.
   - Binding readout: `provider / model / key…` или `Auto (rotation) / model` (данные T1.3/T1.4).
   - Description, Statistics (calls/tokens/latency/errors/lastActive из `agentStats`, `No runs yet` если пусто).
   - Действия: To debate (`navigate('/debate')` + close — агенты топологии уже доступны в DebatePanel), Edit (→ config tab), Duplicate, Reset stats.
2. **Таб `profile`**: `TabId` + `sidebar-tabs` (первый, иконка Info) + render branch; i18n `agents.tab_profile` EN+RU.
3. **Дефолт открытия** `config` → `profile` (карточка первой).

## Проверки (static)
| Check | Evidence | Result |
|-------|----------|--------|
| Все пропсы существуют | agentStats/onDuplicateAgent/onResetAgentStats/onSetActiveTab/onClose — все в `AgentDetailPanelProps` | ✅ |
| Binding читает T1.3-поля | `agent.providerId/model/keyId` из маппинга контейнера | ✅ |
| Хедер-дубликаты не трогали | Duplicate/Delete в хедере остались | ✅ |
| “Test” не делали | нет single-runner для агента топологии — честно отсутствует, кандидатура на будущее | ⏳ |
| Контракты/сторы/роуты | не тронуты | ✅ |

## Как проверить
1. Открыть агента → сразу Profile: характеристики, binding, статистика.
2. Пинованый агент показывает `groq / maverick / key…`, ротационный — `Auto (rotation)`.
3. To debate → `/debate`, агент уже в списке доступных.
4. Edit → config tab; Duplicate/Reset stats работают как раньше.

**STOP — T3 done (3.1+3.2). Дальше T4 (opencode research) только по GO.**
