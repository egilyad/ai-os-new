import { describe, it, expect } from 'vitest';
describe('AudienceService',()=>{
    it('placeholder',()=>{expect(true).toBe(true)});
    it('true',()=>{expect(1).toBe(1)});
    it('not throw',()=>{expect(()=>{}).not.toThrow()});
});
